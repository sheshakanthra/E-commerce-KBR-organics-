# Preview Website

I have successfully started the development server and previewed the website.

## Actions Taken
1.  **Explored Project Structure**: Verified the existence of `frontend` and `backend` directories.
2.  **Started Development Server**: Ran `npm run dev` in the `frontend` directory.
3.  **Previewed Website**: Accessed `http://localhost:5173` and verified the content loads correctly.

## Status
The website is running at `http://localhost:5173`.
